#include <stdio.h>
#include <stdlib.h>

/*ola primeiro teste em C receba ksks*/
// uma linha

int main (int args, char *argv[]) {
    int abacaxi, laranja, total;
    // Entrada de Dados
    laranja = 4;
    abacaxi = 3;

    // Processamento de Dados
    total = laranja + abacaxi;

    // Saída de Dados
    printf("%d", total);
    return 0;
}
